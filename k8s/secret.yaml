# Kubernetes Secret for OpenAI API Key
apiVersion: v1
kind: Secret
metadata:
  name: openai-secret
type: Opaque
data:
  # Base64 encoded API key
  # To create: echo -n "your-api-key" | base64
  api-key: ""
